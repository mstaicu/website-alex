import React from 'react';

import styled from '@emotion/styled';

const DigitalFootprint = () => <Wrapper>Digital footprint in progress</Wrapper>;

const Wrapper = styled('div')``;

export default DigitalFootprint;
